<h1>Pricing</h1>
<?php $moduleTotal=0; foreach($modules as $m){ if(!empty($m['is_core_included'])) continue; $moduleTotal += \App\Services\PricingService::resolveModulePrice($m,$cycle);} ?>
<div class="card">Billing cycle: <a href="/pricing?cycle=monthly">Monthly</a> | <a href="/pricing?cycle=quarterly">Quarterly</a> | <a href="/pricing?cycle=yearly">Yearly</a></div>
<div class="grid grid-3"><?php foreach($plans as $p): $price=\App\Services\PricingService::resolvePlanPrice($p,$cycle); ?><div class="card"><h3><?= htmlspecialchars($p['plan_name']) ?> <?= !empty($p['recommended_flag'])?'<span class="badge">Recommended</span>':'' ?></h3><p>₹<?= number_format($price,2) ?>/<?= htmlspecialchars($cycle) ?></p><p>Trial: <?= (int)$p['trial_days'] ?> days</p><ul><?php foreach($p['included_modules'] as $m): ?><li><?= htmlspecialchars($m) ?></li><?php endforeach; ?></ul></div><?php endforeach; ?></div>
<div class="card">Individual paid modules total: ₹<?= number_format($moduleTotal,2) ?> / <?= htmlspecialchars($cycle) ?>. Bundle pricing is intentionally lower. Free trial starts after approval. <a href="/signup/pm-surya-ghar">Start Signup</a></div>
